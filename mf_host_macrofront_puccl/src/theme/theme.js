import { createTheme } from "@mui/material/styles";

export const pucclTheme = createTheme({
    palette: {
        primary: {
            main: '#833177',
        },
        secondary: {
          main: '#F5F5F5',
          contrastText: '#fff',
        },
        footer: {
            main: '#EEEEEE'
        }
    },
});